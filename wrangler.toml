name = "paramountindex"
compatibility_date = "2025-04-01"

# Workers + Assets mode — matches Cloudflare's configured deploy command
# `npx wrangler versions upload` needs [assets] to find static files
[assets]
directory = "client/dist"
not_found_handling = "single-page-application"
